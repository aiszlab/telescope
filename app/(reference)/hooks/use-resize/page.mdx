import Usage from '@/components/usage'

# useResize

监听 `window` 尺寸变化（`resize`）的钩子。

## 代码演示

<Usage
  sources={[
    {
      src: '/hooks/use-resize/basic.tsx',
      title: '基本',
      description: '调整窗口大小时计数。'
    }
  ]}
/>

## API

### Parameters

| 参数     | 说明           | 类型       | 默认值 |
| -------- | -------------- | ---------- | ------ |
| callback | resize 回调函数 | `Function` | -      |

### Return

无。

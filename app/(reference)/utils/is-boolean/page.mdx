## isBoolean 判断布尔值

## 使用场景

判断值是否为 `boolean` 类型

## 代码演示

```ts
import { isBoolean } from '@aiszlab/relax'

isBoolean(true) // => true
isBoolean(false) // => true
isBoolean(0) // => false
isBoolean('true') // => false
```

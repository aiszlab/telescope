## max 最大值

## 使用场景

从数组中获取最大值，支持通过 `pipe` 函数自定义比较逻辑

## 代码演示

```ts
import { max } from '@aiszlab/relax'

max([1, 2, 3, 4, 5]) // => 5

// 自定义比较逻辑
max(
  [{ age: 18 }, { age: 25 }, { age: 12 }],
  (item) => item.age
) // => { age: 25 }
```

## merge 深度合并

## 使用场景

深度合并多个对象，使用唯一值避免重复。常用于合并配置项、对象状态更新等场景

## 代码演示

```ts
import { merge } from '@aiszlab/relax'

merge(
  { a: 1, b: { x: 1 } },
  { b: { y: 2 }, c: 3 }
)
// => { a: 1, b: { x: 1, y: 2 }, c: 3 }
```

## min 最小值

## 使用场景

从数组中获取最小值，支持通过 `pipe` 函数自定义比较逻辑

## 代码演示

```ts
import { min } from '@aiszlab/relax'

min([1, 2, 3, 4, 5]) // => 1

// 自定义比较逻辑
min(
  [{ age: 18 }, { age: 25 }, { age: 12 }],
  (item) => item.age
) // => { age: 12 }
```

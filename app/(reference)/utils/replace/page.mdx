## replace 替换

## 使用场景

统一的替换工具，支持字符串替换和数组元素替换。字符串替换为 `String.prototype.replace` 的封装，数组替换将匹配的元素替换为新值

## 代码演示

```ts
import { replace } from '@aiszlab/relax'

// 字符串替换
replace('hello world', 'world', 'there') // => 'hello there'

// 数组替换
replace([1, 2, 3, 2, 4], 2, 9) // => [1, 9, 3, 9, 4]
```

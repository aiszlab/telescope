## toggle 切换数组元素

## 使用场景

在数组中切换某个元素的存在状态：如果存在则移除，如果不存在则添加。返回一个新数组

## 代码演示

```ts
import { toggle } from '@aiszlab/relax'

toggle([1, 2, 3], 2) // => [1, 3]
toggle([1, 2, 3], 4) // => [1, 2, 3, 4]
toggle(new Set([1, 2, 3]), 2) // => Set { 1, 3 }
```

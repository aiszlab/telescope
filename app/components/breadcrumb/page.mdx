import Usage from '@/components/usage'

# Breadcrumb 面包屑

显示当前页面在系统层级结构中的位置，并能向上返回。

## 何时使用

- 当系统拥有超过两级以上的层级结构时；
- 当需要告知用户『你在哪里』时；
- 当需要向上导航的功能时。

## 代码演示

<Usage
  small
  sources={[
    {
      src: '/components/breadcrumb/basic.tsx',
      title: '基本',
      description: '最简单的用法。'
    },
    {
      src: '/components/breadcrumb/separator.tsx',
      title: '自定义间隔符',
      description: '自定义间隔符。'
    }
  ]}
/>

## API

### Breadcrumb

| 参数      | 说明         | 类型               | 默认值 |
| --------- | ------------ | ------------------ | ------ |
| items     | 路由栈信息   | `BreadcrumbItem[]` | `[]`   |
| separator | 自定义分隔符 | `React.ReactNode`  | `/`    |

### BreadcrumbItem

```ts
type BreadcrumbItem = {
  /**
   * @description
   * item display label
   */
  label: React.ReactNode

  /**
   * @description
   * item href link
   * if link is not provided, item will display as a label
   */
  href?: string
}
```

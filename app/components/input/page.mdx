import Basic from './mocks/basic'
import basicSource from './mocks/basic?raw'
import AddonAll from './mocks/addon-all'
import addonAllSource from './mocks/addon-all?raw'
import Password from './mocks/password'
import passwordSource from './mocks/password?raw'

# Input 输入框

通过鼠标或键盘输入内容，是最基础的表单域的包装。

## 何时使用

- 需要用户输入表单域内容时。
- 提供组合型输入框，带搜索的输入框，还可以进行大小选择。

## 代码演示

<Playable player={<Basic />} source={basicSource} title='基本' description='最简单的用法。' />
<Playable
  player={<AddonAll />}
  source={addonAllSource}
  title='前缀和后缀'
  description='在输入框上添加前缀或后缀图标。'
/>
<Playable player={<Password />} source={passwordSource} title='密码框' description='密码框。' />

## API

### Input

| 参数     | 说明                   | 类型                 | 默认值   |
| -------- | ---------------------- | -------------------- | -------- |
| value    | 输入框内容             | `string`             | `void 0` |
| onChange | 输入框内容变化时的回调 | `() => void`         | `void 0` |
| prefix   | 输入框前缀             | `ReactNode`          | `void 0` |
| suffix   | 输入框后缀             | `ReactNode`          | `void 0` |
| type     | 输入框类型             | `text` \| `password` | `void 0` |

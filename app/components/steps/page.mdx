import Usage from '@/components/usage'

# Steps 步骤条

## 何时使用

步骤条，一般用于完成一个任务需要分几个步骤，在每个步骤完成之前，用户必须完成前一个步骤。

## 代码演示

<Usage
  sources={[
    {
      src: '/components/steps/basic.tsx',
      title: '基本',
      description: '最简单的用法。'
    }
  ]}
/>

## API

| 参数     | 说明             | 类型                           | 默认值   |
| -------- | ---------------- | ------------------------------ | -------- |
| gutter   | 子元素之间的边距 | `number` \| `[number, number]` | `4`      |
| children | 子元素           | `ReactNode`                    | `void 0` |

'use client'
import { Upload, Button } from 'musae'

const App = () => {
  return (
    <Upload onClick={() => {}} uploader={async () => 'test'}>
      <Button>Upload</Button>
    </Upload>
  )
}

export default App
